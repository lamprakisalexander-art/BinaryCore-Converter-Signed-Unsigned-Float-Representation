# BinaryCore-Converter-Signed-Unsigned-Float-Representation
This Python program converts numbers to binary manually. It handles integers, floating-point numbers, and negative numbers (using two’s complement with a chosen bit size) and prints their binary representations while checking for possible overflow.
